{
  "name": "sora",
  "version": "0.1.0",
  "private": true,
  "description": "Open-source AI agent runtime and workspace",
  "type": "module",
  "workspaces": ["packages/*", "cli"],
  "scripts": {
    "sora": "bun cli/src/bin.ts",
    "test": "bun test",
    "typecheck": "bunx tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.8.2"
  }
}
