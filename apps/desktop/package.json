{
  "name": "@sora/desktop",
  "version": "0.1.2",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "bunx tauri dev",
    "dev": "bunx tauri dev",
    "build": "bunx tauri build",
    "dev:xwin": "bunx tauri dev --runner cargo-xwin",
    "build:xwin": "bunx tauri build --runner cargo-xwin"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  },
  "dependencies": {
    "@tauri-apps/api": "^2"
  }
}
