# Do not set [build].target here — it breaks macOS/Linux CI.
# Windows CI passes --target x86_64-pc-windows-msvc via tauri-action.
# Local Windows: use the MSVC toolchain (default rustup on Windows).
