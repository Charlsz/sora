# Sora

Local-first desktop agent workspace. Tauri + Bun. BYO models.

```bash
bun install
bun run sora init
bun run desktop
```

Windows (no VS): install LLVM, enable Developer Mode, keep `cargo-xwin` (desktop scripts use it).

CLI: `bun run sora --help` · State: `~/.sora`
